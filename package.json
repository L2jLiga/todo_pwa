{
  "name": "test",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://l2jliga.github.io/todo_pwa/",
  "dependencies": {
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "react-scripts": "2.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "rm -Rf docs && react-scripts build && mv build docs && cp docs/index.html docs/404.html && git add . && git commit -m \"Updated docs\" && git push",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
